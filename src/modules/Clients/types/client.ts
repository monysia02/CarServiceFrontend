export type ClientFormFields = {
  id?: string;
  name: string;
  surName: string;
  phoneNumber: string;
};

export type Client = {
  clientId: string;
  name: string;
  surName: string;
  phoneNumber: string;
};
