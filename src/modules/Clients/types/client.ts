export type ClientFormFields = {
  id?: string;
  name: string;
  surName: string;
  phoneNumber: string;
};

export type Client = {
  customerId: string;
  name: string;
  surName: string;
  phoneNumber: string;
};
